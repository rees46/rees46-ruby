# frozen_string_literal: true

RSpec.describe Rees46 do
  it "has a version number" do
    expect(Rees46::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
